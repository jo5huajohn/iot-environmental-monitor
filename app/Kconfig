# SPDX-License-Identifier: Apache-2.0

menu "IoT Environmental Monitor"

config SENSOR_READ_INTERVAL
	int "Sensor read interval (seconds)"
	default 60
	help
	  Interval in seconds between sensor readings.

endmenu

source "Kconfig.zephyr"
